spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:waitListTest;DB_CLOSE_DELAY=-1;NON_KEYWORDS=KEY,VALUE
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        hbm2ddl:
          auto: create

